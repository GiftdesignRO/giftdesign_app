import 'package:flutter/material.dart';

const primaryColor = Color(0xFFC61B2A);
const apiBaseUrl = String.fromEnvironment(
  'API_BASE_URL',
  defaultValue: 'https://giftdesign-api.onrender.com',
);
